# Editable-React-Tables
An application in which you can create, view, edit, tables

# Install
Server
$ cd server
$ npm install

Client
$ cd client
$ npm install

Run & Start
Server
$ cd server

server will start at http://localhost:2725/